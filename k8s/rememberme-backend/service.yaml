apiVersion: v1
kind: Service
metadata:
  name: rememberme-backend-svc
  namespace: rememberme
  labels:
    app: rememberme-backend
spec:
  type: ClusterIP
  selector:
    app: rememberme-backend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
